Postorder = (root: Node? -> v: (Int)List) {
    ? root == nil {
        <- (Int)List_of()
    }
    list = (Int)List_of()
    @ root.children => ea {
        list += Postorder(ea)
    }
    list += root.val
    <- list
}
