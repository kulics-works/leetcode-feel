Transpose := (a: [list [list int]] -> v: [list [list int]]) {
    !L1 := a.len
    !L2 := a[0].len
    !Result := [list [list int]]{}
    0 ..> L2 @ i {
        !temp := [list int]{}
        0 ..> L1 @ j {
            temp += a[j][i]
        }
        Result += temp 
    }
    <- Result
}
