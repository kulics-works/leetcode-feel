"algorithm" {
}
MaxDepth(root: ?TreeNode -> v: Int) {
    ? root == Nil { 
        <- 0
    }

    L := 1 + MaxDepth(root.left)
    R := 1 + MaxDepth(root.right)

    ? L > R { 
        <- L
    } _ {
        <- R
    }
}
