Solution
{
    .. XyLang\Library

    maxDepth (root:TreeNode)->(v:I32)
    {
        ? root == null { <- (0) }

        L := 1 + maxDepth.(root.Left)
        R := 1 + maxDepth.(root.Right)

        ? L > R 
        { <- (L) }
        ? 
        { <- (R) }
    }
}