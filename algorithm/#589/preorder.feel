Preorder := (root: Node? -> v: list int) {
    root == nil ? {
        <- list int{}
    }
    !List := list int{}
    List += root.val
    root.children @ ea {
        List += Preorder(ea)
    }
    <- List
}
