Preorder = (root: Node? -> v: (Int)List) {
    ? root == nil {
        <- (Int)List_of()
    }
    list = (Int)List_of()
    list += root.val
    @ root.children => ea {
        list += Preorder(ea)
    }
    <- list
}
