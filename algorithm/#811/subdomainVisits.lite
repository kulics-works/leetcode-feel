"algorithm" {
}
SubdomainVisits(cpdomains: []Str -> v: []Str) {
    Result := []Str{}
    TempDic := [Str]Int{}
    cpdomains @ ea {
        temp := ea.split(" ")
        count := temp[0].toInt()
        arr := temp[1].split(".")
        arr @ [i]v {
            records := arr[i<=].join(".")
            TempDic[records] += count
        }
    }
    TempDic @ [k]v {
        Result += v.toStr() + " " + k
    }
    <- Result
}
