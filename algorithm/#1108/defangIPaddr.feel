defangIPaddr := (address: str->v: str) {
    !newAddress: list byte
    address @ i {
        i == '.' ? {
            newAddress += {'['; '.'; ']'}
        } _ {
            newAddress += i
        }
    }
    newAddress.to_str()
}