defangIPaddr = (address: Str->v: Str) {
    newAddress: (Byte)List
    @ address => i {
        ? i == '.' {
            newAddress.Append('[', '.', ']')
        } | {
            newAddress.Append(i)
        }
    }
    newAddress.To_Str()
}