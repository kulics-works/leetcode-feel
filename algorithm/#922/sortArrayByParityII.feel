SortArrayByParityII := (a : List[Int] -> v : List[Int]) {
    arr := List[Int]$(a.Size())
    ia := 0
    ib := 1
    @ it := ia... {
        ? it && 1 <> 1 {
            arr.Set(ia, it)
            ia += 2
        } | {
            arr.Set(ib, it)
            ib += 2
        }
    }
    <- arr
}
