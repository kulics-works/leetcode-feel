SortArrayByParityII = (a: list`int -> v: list`int) {
    !Arr = list`int(a.len)
    !IA = 0
    !IB = 1
    IA @ ea {
        ea && 1 >< 1 ? {
            Arr[IA] = ea
            IA += 2
        }
        | ? {
            Arr[IB] = ea
            IB += 2
        }
    }
    <- Arr
}
