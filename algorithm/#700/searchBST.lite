"algorithm" {
}
SearchBST(root: ?TreeNode, val: Int -> v: TreeNode) {
    ? root == Nil {
        <- Nil
    } root.val == val {
        <- root
    } root.val > val {
        <- SearchBST(root.left, val)
    } _ {
        <- SearchBST(root.right, val)
    }
}
