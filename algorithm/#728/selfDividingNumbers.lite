"algorithm" {
}
SelfDividingNumbers(left: Int, right: Int -> v: []Int) {
    Arr := []Int{}
    [left <= right] @ i {
        TempStr := i.toStr()
        TempStr @ v {
            Temp := 0
            v ? '0' { 
                <- @ 
            } _ { 
                Temp = i % v.toInt() 
            }

            ? Temp >< 0 {
                <- @
            }
        }
        Arr += i
    }
    <- Arr
}
