SelfDividingNumbers = (left: Int, right: Int -> v: (Int)List) {
    ret = (Int)List_of()
    @ left ... right => i {
        temp_Str = i.To_Str()
        @ temp_Str => v {
            temp = 0
            ? v == '0' { 
                ~@
            } | { 
                temp = i % v.To_I32() 
            }

            ? temp <> 0 {
                ~@
            }
        }
        ret += i
    }
    <- ret
}
