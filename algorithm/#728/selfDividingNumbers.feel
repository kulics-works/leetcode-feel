SelfDividingNumbers := (left: int, right: int -> v: [list int]) {
    !Arr := [list int]{}
    left .. right @ i {
        !TempStr := i.to_str()
        TempStr @ v {
            !Temp := 0
            v == '0' ? { 
                ~@
            } _ { 
                Temp = i % v.to_i32() 
            }

            Temp >< 0 ? {
                ~@
            }
        }
        Arr += i
    }
    <- Arr
}
