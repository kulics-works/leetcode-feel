` Definition for a binary tree node. `
TreeNode = $ (
    val: Int,
    left: TreeNode?,
    right: TreeNode?
)

InorderTraversal = (root: TreeNode?->v: (Int)List) {
    ? root == nil {
        <- nil
    }
    arr = (Int)List_of()
    temp = InorderTraversal(root.left)
    ? temp <> nil {
        arr.Append(temp)
    }
    arr.Append(root.val)
    temp = InorderTraversal(root.right)
    ? temp <> nil {
        arr.Append(temp)
    }
    <- arr
}