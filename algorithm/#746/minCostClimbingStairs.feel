minCostClimbingStairs := (cost: [list int]->v: int) {
    cost.len == 0 ? {
        <- 0
    }
    dp := [list int](cost.len)
    dp[0] = cost[0]
    dp[1] = cost[1]
    2 .. cost.len @ i {
        dp[i] = cost[i] + min(dp[i-1], dp[i-2])
    }
    <- min(dp[cost.len-1], dp[cost.len-2])
}