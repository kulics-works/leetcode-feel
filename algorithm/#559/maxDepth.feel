MaxDepth = (root: ?Node -> v: Int) {
    ? root == nil {
        <- 0
    }

    depth = 0
    @ root.children => ea {
        temp = MaxDepth(ea)
        ? temp > depth {
            depth = temp
        }
    }
    <- depth + 1
}
