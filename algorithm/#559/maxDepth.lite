"algorithm" {
}
MaxDepth(root ?Node -> v int) {
    ? root == nil {
        <- 0
    }

    Depth = 0
    root.children @ ea {
        Temp = MaxDepth(ea)
        ? Temp > Depth {
            Depth = Temp
        }
    }
    <- Depth+1
}
