FizzBuzz = (n: Int -> v: (Str)List) {
    arr = (Str)List_of()

    @ 1 ... n => ea {
        ? ea % 15 == 0 {
            arr += "FizzBuzz"
        } | ea % 3 == 0 {
            arr += "Fizz"
        } | ea % 5 == 0 {
            arr += "Buzz"
        } | {
            arr += ea.To_Str()
        }
    }
    <- arr
}
