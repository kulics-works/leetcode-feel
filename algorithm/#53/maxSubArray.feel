maxSubArray = (nums: (Int)List->v: Int) {
    currentSum = nums[0]
    maxSum = nums[0]
    @ 1 .. nums.Size() => i {
        currentSum = Max(nums[i], currentSum + nums[i])
        maxSum = Max(maxSum, currentSum)
    }
    <- maxSum
}
