maxSubArray = (nums: list`int->v: int) {
    !currentSum = nums[0]
    !maxSum = nums[0]
    1 ..< nums.len @ i {
        currentSum = max(nums[i], currentSum+nums[i])
        maxSum = max(maxSum, currentSum)
    }
    <- maxSum
}
