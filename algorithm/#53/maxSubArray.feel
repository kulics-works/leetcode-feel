maxSubArray := (nums : List[Int]->v : Int) {
    currentSum := nums.Get(0)
    maxSum := nums.Get(0)
    @ i := 1.Up_until(nums.Size())... {
        currentSum = Max(nums.Get(i), currentSum + nums.Get(i))
        maxSum = Max(maxSum, currentSum)
    }
    <- maxSum
}
