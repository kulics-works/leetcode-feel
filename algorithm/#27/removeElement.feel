removeElement = (nums: (Int)List, val: Int -> len: Int) {
    length = nums.Size()
    @ length - 1 ... 0 ~ -1 => i {
        ? nums[i] == val {
            nums.Remove(i)
        }
    }
    <- nums.Size()
}
