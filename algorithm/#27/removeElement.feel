removeElement := (nums : List[Int], val : Int -> len : Int) {
    length := nums.Size()
    @ i := (length - 1).Down_to(0)... {
        ? nums.Get(i) == val {
            nums.Remove(i)
        }
    }
    <- nums.Size()
}
